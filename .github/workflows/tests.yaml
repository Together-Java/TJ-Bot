name: tests

on: [push, pull_request]

jobs:
  tests:
    name: "tests"
    runs-on: ubuntu-latest
    steps:
      - name: Set up JDK
        uses: actions/setup-java@v1
        with:
          java-version: 16
      - uses: actions/checkout@v2
      - name: Check
        run: ./gradlew test
